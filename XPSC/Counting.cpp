#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;

    // masrafi000
    int count = 0;
    while (a<=b)
    {
        /* code */
        count++;
        a++;
    }
    
    // for (int i = a; i <= b; i++)
    // {
    //     count++;
    // }

    cout << count << endl;

    return 0;
}